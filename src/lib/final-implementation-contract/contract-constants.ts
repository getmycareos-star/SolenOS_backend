/** SolenOS Final Implementation Contract — unified canonical pipeline. */

export const SYSTEM_TYPE =
  "a deterministic cognitive compression engine for irreversible, high-uncertainty responsibility contexts";

export const FINAL_IMPLEMENTATION_TRUTH =
  "SolenOS reduces cognitive and emotional load from uncertainty and responsibility loops — it observes surface signals for measurement but never intervenes, and does not deepen understanding, expand reasoning, optimize decisions, generate plans, or simulate alternatives.";

/** Final system spec one-line truth — aligned with cognitive-compression module. */
export const FINAL_SYSTEM_TRUTH = FINAL_IMPLEMENTATION_TRUTH;

export const GLOBAL_EXECUTION_PIPELINE = [
  "INPUT RECEIVED",
  "INPUT CLASSIFICATION",
  "URGENCY DETECTION",
  "SIGNAL EXTRACTION",
  "MODE CONSTRAINT SELECTION",
  "SAFETY OVERRIDE",
  "OUTPUT CONTRACT ENFORCEMENT",
  "STRUCTURED RESPONSE GENERATION",
  "RESPONSE VALIDATION",
  "RELIEF + SIGNAL LOGGING",
] as const;

export const OBSERVATION_PRINCIPLE =
  "Observation allowed — intervention NOT. Surface signals are telemetry labels only; they do NOT route lifecycle, branch UX, change output schema, or trigger behavioral intervention.";

export const FINAL_IMPLEMENTATION_OUTPUT_FIELDS = [
  "what_is_happening",
  "what_matters_now",
  "what_to_ask_next",
  "risk_level",
  "what_can_wait",
] as const;

export const FINAL_IMPLEMENTATION_FORBIDDEN_SYSTEM_TYPES = [
  "chatbot",
  "assistant",
  "care system",
  "analytics product",
  "workflow engine",
  "personalization system",
  "burnout intervention system",
  "caregiver coaching platform",
  "lifecycle router",
  "profiling engine",
] as const;
