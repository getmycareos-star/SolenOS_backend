import type { BehaviorProfile } from "./behavior-profile";

/** Apply safety constraints from behavior profile — does not interpret input. */
export function applySafetyConstraints(profile: BehaviorProfile): BehaviorProfile {
  return profile;
}
