// Optional SDK type declarations.
// Populated when @google-cloud/text-to-speech or @aws-sdk/client-polly
// type packages are installed; otherwise left empty so the rest of the
// module tree still type-checks.
