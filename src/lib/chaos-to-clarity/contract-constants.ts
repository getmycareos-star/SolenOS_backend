/**
 * Chaos-to-Clarity Transformation Engine — contract constants.
 */
export const CHAOS_TO_CLARITY_IDENTITY =
  "a deterministic chaos-to-structured-clarity transformation engine that converts unstructured caregiver input into strictly grounded clarity outputs without inference";

export const CHAOS_TO_CLARITY_ONE_LINE_TRUTH =
  "SolenOS is a deterministic chaos-to-clarity system that transforms unstructured caregiver input into strictly grounded structured outputs while preserving uncertainty and explicitly refusing all inference or narrative completion.";

export const CHAOS_TO_CLARITY_FAILURE_MODEL =
  "SolenOS fails when it adds unstated facts, infers missing context, constructs coherent narrative from fragments, resolves ambiguity artificially, or behaves like a knowledge generator — because the most dangerous failure is false completion of reality.";

export const CHAOS_TO_CLARITY_CORE_PRINCIPLE =
  "A controlled decomposition system that separates signal, noise, uncertainty, and missing information WITHOUT adding anything new.";

export const CHAOS_TO_CLARITY_BEHAVIOR_MODEL =
  "INPUT (chaos) → SURFACE EXTRACTION (no inference) → UNCERTAINTY TAGGING → CONTRADICTION PRESERVATION → STRUCTURED OUTPUT";

export const CHAOS_TO_CLARITY_INPUT_ASSUMPTIONS = [
  "unstructured",
  "incomplete",
  "emotionally influenced",
  "potentially inconsistent",
  "partial signal + noise mix",
] as const;

export const CHAOS_TO_CLARITY_PROCESSING_PIPELINE = [
  "Surface extraction (grounding only)",
  "Uncertainty tagging",
  "Contradiction preservation",
  "Structured transformation",
] as const;

export const CHAOS_TO_CLARITY_FORBIDDEN_IDENTITIES = [
  "parser",
  "summarizer",
  "reasoning engine",
  "knowledge completion system",
  "narrative generator",
] as const;

export const CHAOS_TO_CLARITY_CORE_OUTPUT_BLOCKS = [
  "what_is_happening",
  "what_matters_now",
  "what_to_ask_next",
  "risk_level",
  "what_can_wait",
  "follow_up_items",
] as const;
